# Instructions for LangChain Skill
- Use `LangChain Expression Language (LCEL)` for composing chains.
- Prefer `ChatOpenAI` or `ChatAnthropic` classes for chat models.
- Implement robust error handling for API calls.
- Use `RecursiveCharacterTextSplitter` for document processing.
- Ensure state is managed correctly when using Memory components.