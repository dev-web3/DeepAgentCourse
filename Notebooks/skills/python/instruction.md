# Instructions for Python Skill
- Use type hints for all function signatures.
- Prefer list comprehensions or generator expressions over simple loops where readable.
- Follow PEP 8 style guidelines.
- Use `logging` instead of `print` for production-grade code.
- Always include docstring in Google or NumPy format.
- Handle exceptions specifically; avoid bare `except:`.